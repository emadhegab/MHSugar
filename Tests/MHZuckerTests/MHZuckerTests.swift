import XCTest
@testable import MHZucker

final class MHZuckerTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MHZucker().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
